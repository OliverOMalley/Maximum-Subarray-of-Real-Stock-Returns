# Maximum-Subarray-of-Real-Stock-Returns

This code uses Kadane's algorithm to find the best period of continuous growth in the stock market.
So basically finds the maximum subarray , meaning the maximum subarray of daily returns rather than just the difference between the highest price and lowest price.
